/** Login component, which displays login form for non-logged-in user
 * 
 * Props:
 *  Login function : Calls function in App component to login a user via the API
 * 
 * State: None
 * 
 * App -> Routes -> Login
 */
function Login() {
    return (<p>LOGIN PAGE</p>);
}

export default Login;